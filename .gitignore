# Kesif ciktilari — MAC, ICCID, IMSI, IMEI, nvram tam yedegi icerir. ASLA commit edilmez.
data/
*.nvrambak.bin

# Ortam / sirlar
.env
env
*.env
!.env.example

node_modules/
*.tgz

# ARSIV — repoda yeri yok, diskte kalir. Icinde: eski nvram/izleme anlik
# goruntuleri, modem arayuzu ekran goruntuleri, ham terminal dokumleri
# (parola hash + kimlik icerir). Hicbiri kod ya da test tarafindan
# KULLANILMIYOR (2026-08-28 referans taramasiyla dogrulandi).
arsiv/
